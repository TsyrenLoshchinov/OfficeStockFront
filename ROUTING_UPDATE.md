# Обновление роутинга и навигации

## ✅ Что было сделано

### 1. Обновлен роутинг

**Главная страница (`/app`)**:
- Теперь показывает страницу загрузки чека (ReceiptsPageComponent)
- Доступ: `hr-manager`

**Страница списка чеков (`/app/receipts`)**:
- Показывает список всех чеков (ChequesListComponent)
- Доступ: `hr-manager`, `admin`

### 2. Обновлен Sidebar

- **"Главная"** → `/app` (страница загрузки чека)
- **"Чеки"** → `/app/receipts` (список чеков)

### 3. Создано модальное окно

**Компонент**: `src/app/shared/components/receipt-modal/receipt-modal.component.ts`

**Функциональность**:
- Просмотр информации о чеке
- Режим только чтения (`isReadOnly: true`) для списка чеков
- Режим редактирования для страницы загрузки
- Закрытие по клику на overlay или кнопку закрытия

### 4. Интеграция модального окна

В компоненте `ChequesListComponent`:
- При нажатии "Подробнее" загружаются полные данные чека
- Открывается модальное окно с информацией
- Отображение товаров, категорий, сумм

## 📋 Структура маршрутов

```
/app                    → Загрузка чека (главная для hr-manager)
/app/receipts          → Список чеков
/app/receipts/upload   → (не используется, можно удалить)
```

## 🎯 Навигация

### HR-менеджер:
1. **Главная** (`/app`) → Страница загрузки чека
2. **Чеки** (`/app/receipts`) → Список чеков
3. При нажатии "Подробнее" → Модальное окно с информацией

### Администратор:
1. **Чеки** (`/app/receipts`) → Список чеков
2. При нажатии "Подробнее" → Модальное окно с информацией

## 🔄 Workflow

1. Пользователь заходит на главную → видит форму загрузки чека
2. Загружает чек → данные обрабатываются → показывается предпросмотр
3. Подтверждает чек → чек сохраняется
4. Переходит в "Чеки" → видит список всех чеков
5. Нажимает "Подробнее" → открывается модальное окно с полной информацией

## ✅ Готово к использованию

Все изменения применены и готовы к тестированию!

