export interface AnalyticsPoint {
  label: string;
  value: number;
}

export interface ExpenseStructure {
  category: string;
  percent: number;
}

export interface ExpenseDynamics {
  period: string;
  amount: number;
}

