# Страница списка чеков

## ✅ Что создано

### 1. Компонент списка чеков
- **Файл**: `src/app/features/receipts/cheques-list/cheques-list.component.ts`
- **Функциональность**:
  - Загрузка списка чеков
  - Сортировка (сначала новые / сначала старые)
  - Удаление чека
  - Удаление всех чеков
  - Просмотр деталей чека
  - Использование Angular Signals для реактивности

### 2. Сервис для работы с чеками
- **Файл**: `src/app/features/receipts/services/cheques.service.ts`
- **Методы**:
  - `getCheques()` - получение списка чеков
  - `deleteCheque(id)` - удаление чека
  - `deleteAllCheques()` - удаление всех чеков
  - `getChequeById(id)` - получение чека по ID
- **Mock данные**: Включены для разработки без бэкенда

### 3. Модель данных
- **Файл**: `src/app/core/models/cheque.model.ts`
- **Интерфейс**: `Cheque` с полями:
  - `id`, `number`, `date`, `fileName`, `uploadedAt`
  - `organization`, `totalAmount`, `status`

### 4. Маршрутизация
- **Маршрут**: `/app/receipts`
- **Доступ**: `hr-manager`, `admin`
- **Файл**: `src/app/app.routes.ts`

### 5. Навигация в Sidebar
- Кнопка "Чеки" в sidebar ведет на `/app/receipts`
- Автоматически фильтруется по ролям

## 🎯 Функциональность

### Сортировка
- **Сначала новые** - по дате загрузки (новые сверху)
- **Сначала старые** - по дате загрузки (старые сверху)
- Используется Angular Signals для реактивной сортировки

### Удаление
- Удаление отдельного чека (кнопка крестик)
- Удаление всех чеков (кнопка "Очистить всё")
- Подтверждение перед удалением

### Просмотр деталей
- Кнопка "Подробнее" ведет на страницу деталей чека
- Маршрут: `/app/receipts/:id` (требует реализации)

## 📋 Mock данные

Для разработки включены тестовые данные:
- 3 примера чеков
- Разные даты и организации
- Разные статусы

## 🔄 Интеграция с бэкендом

Когда будет готов бэкенд, нужно:
1. Установить `useMockAuth: false` в `environment.ts`
2. API endpoints:
   - `GET /api/receipts` - получение списка
   - `DELETE /api/receipts/:id` - удаление чека
   - `DELETE /api/receipts` - удаление всех
   - `GET /api/receipts/:id` - получение чека по ID

## 🎨 Стили

Все стили интегрированы из предоставленной верстки:
- Dropdown для сортировки
- Карточки чеков
- Анимации и hover-эффекты
- Адаптивный дизайн

## ✅ Готово к использованию

Страница полностью готова и работает с mock-данными. При нажатии на "Чеки" в sidebar пользователь перенаправляется на страницу со списком чеков.

