# Тестовые данные для авторизации

## 🔐 Данные для входа

### HR-менеджер (основной модуль)
- **Логин:** `hr_manager`
- **Пароль:** `hr123`
- **Роль:** `hr-manager`
- **Доступ:** Модуль загрузки и обработки чеков

### Экономист
- **Логин:** `economist`
- **Пароль:** `econ123`
- **Роль:** `economist`
- **Доступ:** Страница аналитики

### Директор
- **Логин:** `director`
- **Пароль:** `dir123`
- **Роль:** `director`
- **Доступ:** Страница отчётов

### Администратор
- **Логин:** `admin`
- **Пароль:** `admin123`
- **Роль:** `admin`
- **Доступ:** Управление пользователями

## 📝 Как использовать

1. Запустите приложение:
   ```bash
   npm start
   ```

2. Откройте страницу авторизации: `http://localhost:4200/auth/login`

3. Введите данные одного из пользователей выше

4. После успешного входа вы будете автоматически перенаправлены на страницу согласно вашей роли

## ⚙️ Переключение между mock и реальным бэкендом

### Использование mock-сервиса (по умолчанию)
В файле `src/environments/environment.ts`:
```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  useMockAuth: true  // ← Используется mock-сервис
};
```

### Использование реального бэкенда
В файле `src/environments/environment.ts`:
```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  useMockAuth: false  // ← Используется реальный бэкенд
};
```

## 🔄 Что происходит при авторизации

1. Ввод логина и пароля
2. Проверка данных (mock или реальный бэкенд)
3. Сохранение токена в localStorage
4. Сохранение данных пользователя
5. Автоматический редирект на страницу согласно роли:
   - `hr-manager` → `/hr/receipts`
   - `economist` → `/economist/analytics`
   - `director` → `/director/reports`
   - `admin` → `/admin/users`

## 🧪 Тестирование

Для тестирования модуля HR-менеджера используйте:
- **Логин:** `hr_manager`
- **Пароль:** `hr123`

После входа вы попадёте на страницу загрузки чеков, где сможете:
- Загружать чеки (JPG, JPEG, PNG, PDF)
- Просматривать данные чека
- Редактировать информацию о товарах
- Подтверждать чеки

