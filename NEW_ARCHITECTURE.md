# Новая архитектура проекта OfficeStock

## 📁 Структура проекта

```
src/app/
├── core/                          # Ядро приложения
│   ├── guards/                    # Защита маршрутов
│   │   ├── auth.guard.ts         # Проверка аутентификации
│   │   └── role.guard.ts         # Проверка ролей
│   ├── interceptors/              # HTTP interceptors
│   │   └── auth.interceptor.ts   # Добавление токена
│   ├── services/                  # Базовые сервисы
│   │   ├── auth.service.ts       # Авторизация
│   │   ├── auth-mock.service.ts  # Mock для разработки
│   │   ├── auth.service.provider.ts
│   │   └── api.service.ts        # API конфигурация
│   ├── models/                    # Модели данных
│   │   ├── user.model.ts
│   │   └── receipt.model.ts
│   └── layouts/                   # Layout компоненты
│       └── main-layout/          # Главный layout с sidebar и header
│
├── shared/                        # Общие компоненты
│   └── components/
│       ├── sidebar/              # Боковое меню
│       └── header/               # Шапка приложения
│
├── features/                      # Модули по функциям
│   ├── auth/                     # Авторизация
│   │   └── login/
│   ├── receipts/                 # Работа с чеками
│   │   ├── services/
│   │   ├── upload-receipt/
│   │   ├── receipt-preview/
│   │   └── receipts-page/
│   ├── recognition/              # OCR, QR, дубликаты
│   ├── warehouse/               # Склад, категории
│   ├── analytics/               # Графики, отчёты
│   ├── notifications/           # Уведомления
│   └── admin/                   # Управление пользователями
│
├── app.component.ts
└── app.routes.ts
```

## 🔐 Система авторизации и ролей

### Guards

**AuthGuard** - проверяет аутентификацию пользователя
```typescript
canActivate: [authGuard]
```

**RoleGuard** - проверяет роль пользователя
```typescript
canActivate: [roleGuard],
data: { roles: ['hr-manager', 'admin'] }
```

### Роли и доступ

| Роль | Доступные страницы |
|------|-------------------|
| `hr-manager` | Чеки, Склад, Уведомления |
| `economist` | Аналитика, Склад, Уведомления |
| `director` | Отчёты, Аналитика, Уведомления |
| `admin` | Все страницы + Управление пользователями |

## 🎨 Layout система

### MainLayoutComponent

Все страницы после авторизации используют `MainLayoutComponent`, который включает:
- **Sidebar** - боковое меню с навигацией
- **Header** - шапка с уведомлениями и профилем
- **RouterOutlet** - контент страницы

### Маршруты

- `/auth/login` - без layout (публичная страница)
- `/app/*` - с layout (защищенные страницы)

## 📋 Примеры маршрутов

```typescript
{
  path: 'app/receipts',
  loadComponent: () => import('./features/receipts/...'),
  canActivate: [roleGuard],
  data: { roles: ['hr-manager'] }
}
```

## 🔄 Навигация в Sidebar

Sidebar автоматически фильтрует пункты меню по роли пользователя:

```typescript
menuItems: MenuItem[] = [
  { label: 'Чеки', route: '/app/receipts', roles: ['hr-manager', 'admin'] },
  { label: 'Склад', route: '/app/warehouse', roles: ['hr-manager', 'economist', 'admin'] },
  // ...
];
```

## ✅ Преимущества новой архитектуры

1. **Модульность** - каждый feature изолирован
2. **Переиспользование** - общие компоненты в shared
3. **Масштабируемость** - легко добавлять новые features
4. **Разделение ответственности** - core, shared, features
5. **Единый layout** - все страницы используют один layout
6. **Гибкая система ролей** - RoleGuard с проверкой доступа

## 🚀 Следующие шаги

1. Переместить существующие компоненты в features
2. Создать модули recognition, warehouse, analytics, notifications
3. Реализовать функциональность каждого модуля
4. Добавить обработку ошибок и уведомления

