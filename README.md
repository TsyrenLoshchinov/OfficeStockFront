# OfficeStock — Corporate Purchase Tracking System

Angular-приложение для отслеживания корпоративных покупок с системой авторизации и разграничением ролей.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск приложения

```bash
npm start
```

Приложение будет доступно по адресу: `http://localhost:4200`

## 📁 Структура проекта

```
src/
├── app/
│   ├── auth/                    # Модуль авторизации
│   │   ├── login/               # Компонент страницы входа
│   │   ├── services/            # AuthService
│   │   ├── guards/              # AuthGuard
│   │   └── interceptors/        # HTTP Interceptor для токенов
│   │
│   ├── hr/                      # Модуль HR-менеджера
│   │   └── receipts/            # Работа с чеками
│   │       ├── upload-receipt/  # Компонент загрузки чека
│   │       ├── receipt-preview/ # Компонент предпросмотра чека
│   │       └── receipts-page/   # Главная страница модуля
│   │
│   ├── economist/               # Модуль экономиста
│   ├── director/                # Модуль директора
│   ├── admin/                   # Модуль администратора
│   │
│   ├── core/                    # Ядро приложения
│   │   ├── models/              # Модели данных
│   │   └── services/            # Базовые сервисы
│   │
│   └── shared/                  # Общие компоненты (будущее расширение)
│
├── assets/                      # Статические ресурсы
│   └── icons/                   # Иконки приложения
│
└── environments/                # Конфигурация окружений
```

## 🔐 Система авторизации

### Роли пользователей

- **admin** — Управление пользователями
- **hr-manager** — Загрузка и обработка чеков
- **economist** — Аналитика
- **director** — Отчёты

### API Endpoints

#### Авторизация
- `POST /auth/login` — Вход в систему
  ```json
  {
    "login": "string",
    "password": "string"
  }
  ```
  
  Ответ:
  ```json
  {
    "accessToken": "string",
    "role": "hr-manager" | "economist" | "director" | "admin",
    "userId": 1,
    "username": "string"
  }
  ```

#### Работа с чеками (HR-менеджер)
- `POST /receipts/upload` — Загрузка чека
  - FormData: `{ file: File }`
  
  Ответ:
  ```json
  {
    "organization": "string",
    "purchaseDate": "string",
    "totalAmount": 0,
    "items": [
      {
        "name": "string",
        "quantity": 0,
        "price": 0,
        "category": "string"
      }
    ]
  }
  ```

- `POST /receipts/confirm` — Подтверждение чека
  ```json
  {
    "receiptData": {
      "organization": "string",
      "purchaseDate": "string",
      "totalAmount": 0,
      "items": [...]
    }
  }
  ```

## 🛠️ Настройка

### Конфигурация API

Измените URL бэкенда в файле `src/environments/environment.ts`:

```typescript
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api' // Ваш URL бэкенда
};
```

## 📝 Основные компоненты

### LoginComponent
- Страница авторизации с интеграцией существующей верстки
- Валидация формы
- Автоматический редирект по ролям после входа

### UploadReceiptComponent
- Загрузка файлов чеков (JPG, JPEG, PNG, PDF)
- Валидация формата и размера (макс. 10 МБ)
- Отображение прогресса загрузки
- Интеграция с существующей версткой

### ReceiptPreviewComponent
- Предпросмотр данных чека
- Редактирование названий товаров
- Редактирование количества
- Выбор категории товара
- Подтверждение чека

## 🔒 Guards и Interceptors

### AuthGuard
- Проверка аутентификации
- Проверка ролей пользователя
- Автоматический редирект при отсутствии доступа

### AuthInterceptor
- Автоматическое добавление Bearer токена в заголовки запросов
- Работает для всех HTTP-запросов

## 🎨 Интеграция верстки

Вся существующая верстка из папки `OfficeStockFront` интегрирована в Angular компоненты:
- Сохранены оригинальные стили
- Адаптированы под Angular (директивы, биндинги)
- Добавлена интерактивность через Angular Forms

## 📦 Зависимости

- Angular 17+ (Standalone Components)
- RxJS
- Angular Forms (Reactive Forms)

## 🚧 Будущие улучшения

- [ ] Добавить страницу списка чеков
- [ ] Реализовать модуль аналитики для экономиста
- [ ] Реализовать модуль отчётов для директора
- [ ] Реализовать управление пользователями для администратора
- [ ] Добавить обработку ошибок и уведомления
- [ ] Добавить интернационализацию (i18n)

## 📄 Лицензия

Проект создан для учебных целей.

## 🔐 Тестовые учетные данные

Для входа в систему используйте следующие тестовые аккаунты:

| Роль | Логин | Пароль | Описание доступа |
| :--- | :--- | :--- | :--- |
| **HR-менеджер** | `hr_manager` | `hr123` | Модуль загрузки и обработки чеков |
| **Экономист** | `economist` | `econ123` | Страница аналитики и просмотра данных |
| **Директор** | `director` | `dir123` | Страница сводных отчётов |
| **Администратор** | `admin` | `admin123` | Управление пользователями системы |

> **Примечание:** Для переключения между Mock-данными и реальным API используйте флаг `useMockAuth` в файле `src/environments/environment.ts`.

